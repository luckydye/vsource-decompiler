import fs from 'fs';
import path from 'path';

export default {

    usage: 'source2 <file_name>',
    description: 'Read source2 files.',

    async execute([ filename ]) {
        if(!filename) {
            return;
        }

        
    }
}
